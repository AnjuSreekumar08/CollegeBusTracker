<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">?android:attr/windowBackground</item>
        <item name="colorPrimaryDark">@color/red</item>
        <item name="colorAccent">@color/blueiish</item>
        <item name="android:windowTranslucentStatus">true</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="MyTextView">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Subhead</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <color name="blue">#438AD1</color>
    <color name="red">#3789C4</color>
    <color name="blueiish">#9C27B0</color>

    <style name="SpinnerTheme">
        <item name="android:textColor">@color/themecolor</item>
        <item name="android:fontFamily">@font/roboto_light</item>


    </style>
</resources>